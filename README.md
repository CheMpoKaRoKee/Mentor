# Learning Mentor

**Learning Mentor** — desktop-приложение для самостоятельного изучения разработки с AI-наставником.

Программа помогает проходить учебный маршрут небольшими шагами: читать теорию, выполнять задания, задавать вопросы наставнику, сохранять прогресс и возвращаться к темам, которые требуют повторения.

![Status](https://img.shields.io/badge/status-MVP-blue)
![Platform](https://img.shields.io/badge/platform-Windows-lightgrey)
![Electron](https://img.shields.io/badge/Electron-42.x-47848F)
![React](https://img.shields.io/badge/React-18.x-61DAFB)
![Vite](https://img.shields.io/badge/Vite-8.x-646CFF)

## Быстрый смысл

Самостоятельное обучение часто распадается на разрозненные материалы, хаотичные AI-запросы и отсутствие проверки знаний.

Learning Mentor собирает всё в один повторяемый сценарий:

1. выбрать тему;
2. получить объяснение и примеры;
3. выполнить проверочное задание;
4. задать вопрос AI-наставнику;
5. сохранить прогресс;
6. вернуться к слабым темам позже.

## Portfolio documentation

- [Case Study](docs/case-study.md) — проблема самостоятельного обучения, решение и продуктовый сценарий.
- [Development Guide](DEVELOPMENT.md) — локальный запуск, smoke test и правила изменений.
- [Security Policy](SECURITY.md) — безопасная работа с локальными данными, сборками и конфигурацией.

## Case summary

**Проблема:** самостоятельное обучение часто разваливается из-за отсутствия маршрута, обратной связи и регулярного повторения.

**Решение:** Learning Mentor упаковывает AI-наставника в desktop-приложение с маршрутом, объяснениями, заданиями и сохранением прогресса.

**Результат:** пользователь получает не просто чат, а повторяемый учебный процесс: тема → объяснение → задание → вопрос наставнику → сохранение прогресса.

Подробнее: [Case Study](docs/case-study.md).

## Возможности

- пошаговый маршрут от HTML/CSS к React, Node.js, TypeScript, Docker и деплою;
- генерация теории с примерами кода;
- проверочные задания в формате тестов и задач на код;
- встроенный чат-наставник по текущей теме;
- автоматическое сохранение прогресса;
- повторение тем, которые не были пройдены с первого раза;
- desktop-запуск на Electron;
- portable-режим без установки;
- хранение пользовательских данных рядом с portable-сборкой.

## Стек

| Область | Технологии |
|---|---|
| Desktop | Electron |
| Frontend | React, Vite |
| UI | Tailwind CSS, Lucide React |
| Markdown / code rendering | react-markdown, react-syntax-highlighter |
| AI API | OpenRouter API |
| Сборка | electron-builder |
| Package manager | pnpm |

## Кому подойдет

- начинающим frontend/fullstack-разработчикам;
- тем, кто учится самостоятельно и хочет видеть понятный маршрут;
- студентам, которым нужен тренажёр с теорией, заданиями и повторением;
- разработчикам, которые хотят адаптировать desktop-приложение под свой учебный процесс.

## Что показывает проект как портфолио

Этот репозиторий демонстрирует:

- упаковку AI API в понятный пользовательский сценарий;
- разработку desktop-приложения на Electron;
- сборку интерфейса на React;
- проектирование учебного workflow;
- работу с локальным хранением настроек и прогресса;
- подготовку portable-сборки для Windows;
- оформление проекта для публичной публикации на GitHub.

## Быстрый запуск из исходников

### Требования

- Node.js 20 или новее;
- pnpm;
- OpenRouter API key.

### Установка

```bash
pnpm install
```

Создайте `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Добавьте ключ OpenRouter:

```env
OPENROUTER_API_KEY=your_openrouter_api_key
```

Запуск в режиме разработки:

```bash
pnpm run dev
```

## Сборка приложения

```bash
pnpm run build
```

После сборки файлы создаются в `dist/`. Эта папка не хранится в репозитории.

## Portable-версия

Portable-сборка рассчитана на запуск без установки. Достаточно открыть исполняемый файл приложения.

Особенности portable-режима:

- `portable.flag` включает хранение данных внутри папки `Portable/data`;
- прогресс и настройки создаются после первого запуска;
- папку можно перенести на другой диск или флешку;
- пользовательские данные не должны попадать в git;
- готовую portable-сборку лучше публиковать через GitHub Releases как архив.

Перед публикацией portable-архива проверьте, что внутри нет:

- `.env` с реальным API-ключом;
- `Portable/data` с пользовательскими данными;
- логов, кэша и временных файлов;
- `node_modules`, `dist`, `release` и кэша Electron.

## Настройка OpenRouter

Приложению нужен OpenRouter API key. Его можно указать двумя способами:

1. через окно настроек внутри приложения;
2. через переменную `OPENROUTER_API_KEY` в локальном `.env`.

Файл `.env` не должен попадать в репозиторий. Для примера настроек используется `.env.example`.

## Структура проекта

```text
learning-mentor/
├── src/
│   ├── main/          # главный процесс Electron, хранение данных, OpenRouter API
│   ├── preload/       # безопасный мост между Electron и React
│   └── renderer/      # интерфейс React, экраны и компоненты
├── Portable/          # локальная portable-сборка для релизов
├── docs/              # case study и документация проекта
├── electron-builder.yml
├── package.json
├── pnpm-lock.yaml
└── README.md
```

## Roadmap

- добавить выбор учебного маршрута и уровня сложности;
- расширить набор тем и типов заданий;
- добавить экспорт прогресса;
- улучшить офлайн-режим для уже полученных уроков;
- добавить тесты для логики хранения прогресса;
- оформить первый GitHub Release с installer- и portable-версией.

## Безопасность перед публикацией

Перед `push` или публикацией релиза проверьте:

- `.env` не добавлен в git;
- OpenRouter API key не сохранён в `settings.json`, portable-папке или логах;
- `Portable/data` отсутствует в portable-архиве;
- `node_modules`, `dist`, `release` и кэш Electron не попадают в коммит;
- в README нет приватных данных, токенов и локальных путей пользователя.

Подробно: [Security Policy](SECURITY.md).

## Лицензия

Лицензия пока не выбрана. До добавления отдельного `LICENSE` использование кода регулируется стандартными правилами GitHub для репозиториев без указанной лицензии.

## Статус

Проект находится в стадии MVP. Основная цель текущей версии — показать рабочий desktop-инструмент для самостоятельного обучения с AI-интеграцией и portable-запуском.
